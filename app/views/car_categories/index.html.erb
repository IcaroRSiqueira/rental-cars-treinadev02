<h3>Categorias de Carros</h3>
<% if @car_categories.empty? %>
  <p>Não existem categorias de carro cadastradas no sistema.</p>
<% end %>

<% @car_categories.each do |car_category| %>
  <%= link_to car_category.name, car_category %>
<% end %>
<%= link_to 'Voltar', root_path %><br>

<%= link_to 'Registrar nova categoria de carro', new_car_category_path %>
