<h1>Clientes</h1>
<% if @clients.empty? %>
  <p>Não existem clientes cadastrados no sistema.</p>
<% end %>

<% @clients.each do |client| %>
  <%= link_to client.name, client %>
<% end %>
<%= link_to 'Voltar', root_path %><br>

<%= link_to 'Registrar novo cliente', new_client_path %>
